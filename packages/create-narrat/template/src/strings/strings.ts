import en from "./strings_en.yaml";
import fr from "./strings_fr.yaml";
// Json file as an example if you want to use JSON instead of YAML
import de from "./strings_de.json";

export const strings = {
  en: en.code,
  fr: fr.code,
  de: de,
};
