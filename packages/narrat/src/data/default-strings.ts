import { defaultStringsEn } from './default-strings-en';
import { defaultStringsFr } from './default-strings-fr';

export const defaultLocalizationStrings = {
  en: defaultStringsEn,
  fr: defaultStringsFr,
};
